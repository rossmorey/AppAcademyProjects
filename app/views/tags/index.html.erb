<h1>Tag List</h1>

<ul>
  <% @tags.each do |tag| %>
    <li><%= link_to tag.name, tag_path(tag) %> - <%= link_to "Delete Tag", tag_path(tag), method: :delete, data: {confirm: "Do you really want to delete this tag?"} %></li>
  <% end %>
</ul>
