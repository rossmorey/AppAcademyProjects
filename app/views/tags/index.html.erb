<h1>Tag List</h1>

<ul>
  <% @tags.each do |tag| %>
    <li><%= link_to tag.name, tag_path(tag) %> (<%= tag.articles.count %>) - <%= link_to "delete", tag_path(tag), method: :delete, data: {confirm: "Do you really want to delete this tag?"} %></li>
    <br />
  <% end %>
</ul>
